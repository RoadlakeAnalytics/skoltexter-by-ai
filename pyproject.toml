[tool.black]
line-length = 88
target-version = ["py311"]

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = [
  "E", "W", "F", "I", "B",
  "UP", "RUF"
]
ignore = ["E203", "E501"]
extend-select = ["D"]

[tool.ruff.lint.pydocstyle]
convention = "numpy"

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = [
  # Allow missing docstrings in test classes/functions; keep module docs enforced
  "D101", # Missing docstring in public class
  "D102", # Missing docstring in public method
  "D103", # Missing docstring in public function
  "D106", # Missing docstring in public nested class
]

[tool.pytest.ini_options]
addopts = "-p no:cacheprovider --xdoctest --cov=src --cov=setup_project --cov-report=term-missing"

[tool.mypy]
python_version = "3.13"
strict = true
ignore_missing_imports = true
warn_unused_ignores = true
warn_redundant_casts = true
warn_unreachable = true
disallow_subclassing_any = true
no_implicit_reexport = true
pretty = true
show_error_codes = true
files = [
  "src",
  "setup_project.py",
]
# Exclude optional UI modules from strict static analysis as they depend on
# runtime-only third-party libraries (textual/rich) and are exercised
# separately during integration/UI tests.
exclude = "^src/setup/ui/.*"
